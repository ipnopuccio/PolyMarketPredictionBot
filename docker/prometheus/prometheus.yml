global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - "alerts.yml"

scrape_configs:
  - job_name: "bot"
    metrics_path: /metrics
    static_configs:
      - targets: ["bot:5003"]
        labels:
          service: "btc-bot-v2"
          environment: "production"
